# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor folders
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# Node
node_modules/

# Runtime data (contains buyer PII — never commit)
orders.json
data/
# EXCEPT the events seed script (no PII; needed to seed the DB catalog on deploy)
!data/events_seed.sql

# Local secrets (live API keys — NEVER commit)
.env
.env.*
!.env.example

# Build artifacts (minified output) — dist only; assets/min is committed so
# Render serves the obfuscated JS without needing a build step.
dist/

# Temporary test / helper scripts
_*.js

# Mojibake backup folder (temporary)
.mojibake-backup/

# Temporary helper results
_*result*.txt
_scan_result.txt
_remaining_result.txt
